package com.example.androidexample;

/**
 * An enum for keeping track of what type of points the user wants to see
 */
public enum LeaderboardTimeFrameEnum {
    DAILY,
    WEEKLY,
    MONTHLY,
    YEARLY,
    LIFETIME
}
